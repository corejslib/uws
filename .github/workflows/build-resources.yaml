name: build-resources

on:
  workflow_dispatch:
  release:
    types:
      - published

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/zerocluster/node
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: softvisio/actions/setup-npm@v1

      - uses: softvisio/actions/checkout@v1

      - run: |
          apt-get update
          apt-get install -y git
          cd build
          echo -------------------------
          ls -la ~
          cat ~/.npmrc
          echo ============================
          npm install --ignore-scripts
          npm run build
        shell: bash
